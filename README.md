# Тестовое задание NordCodes (QA Automation)

Проект по автоматизации тестирования игрового API-сервиса. Реализовано полное покрытие единственного эндпоинта согласно предоставленной документации.

## 🛠 Стек технологий
* **Java 17+**
* **JUnit 5** — тестовый движок.
* **RestAssured** — работа с API запросами.
* **WireMock (In-memory)** — имитация ответов внешнего сервиса (запускается автоматически внутри тестов).
* **Owner** — управление конфигурацией проекта (ключи доступа, URL).
* **Java Faker** — генерация уникальных токенов формата HEX (32 символа).
* **Allure** — интерактивная отчетность с наглядным описанием шагов.

## 🏗 Архитектурные особенности
* **Step-based API Client**: Логика запросов инкапсулирована в `AppApi` и `MockApi` с аннотациями `@Step` для автоматического документирования.
* **Double Verification**: Тесты проверяют не только HTTP-статусы, но и бизнес-результат в теле ответа (`result: OK / ERROR`).
* **Security Testing**: Возможность проверки авторизации с подменой статического ключа `X-Api-Key` через перегруженные методы.
* **State Management**: Проверка "состояния" системы — подтверждение того, что токен реально удаляется из хранилища после выполнения `LOGOUT`.

## 📊 Покрытие тестами (Coverage)
Реализовано **12 автоматизированных сценариев**, разделенных на логические группы:

1. **Happy Path (Позитивные сценарии):**
    * Полный успешный цикл пользователя: Вход (`LOGIN`) -> Действие (`ACTION`) -> Выход (`LOGOUT`).
2. **Безопасность (Security):**
    * Проверка блокировки доступа при использовании неверного ключа `X-Api-Key`.
3. **Бизнес-логика и состояния:**
    * Запрет выполнения `ACTION` без предварительного `LOGIN`.
    * Подтверждение удаления токена: запрет `ACTION` после выполнения `LOGOUT`.
    * Ошибка при попытке повторной авторизации (`LOGIN`) уже существующего токена.
4. **Валидация входных данных:**
    * Проверка ограничений на длину токена (строго 32 символа).
    * Проверка допустимых символов (только заглавные буквы A-Z и цифры 0-9).
5. **Устойчивость к сбоям (Mocks):**
    * Обработка критической ошибки (500) от внешнего сервиса авторизации.
    * Обработка превышения времени ожидания (Таймаут 11 сек) от внешнего сервиса.

## 🚀 Инструкция по запуску

> **Важно:** Если у вас установлен Maven или вы запускаете команды через терминал **IntelliJ IDEA**, вы можете использовать стандартную команду `mvn` вместо `./mvnw`.

### Шаг 1. Запуск тестируемого приложения
Убедитесь, что файл `internal-0.0.1-SNAPSHOT.jar` находится в корне проекта. Выполните команду в терминале:
```bash
java -jar -Dsecret=qazWSXedc -Dmock=http://localhost:8888/ internal-0.0.1-SNAPSHOT.jar
```
### Шаг 2. Запуск автоматических тестов
Откройте второе (новое) окно терминала в той же папке проекта и введите одну из команд.
- Для macOS или Linux:
```bash
./mvnw clean test
```
- Для Windows (PowerShell/CMD):
```bash
.\mvnw.cmd clean test
```
### Шаг 3. Просмотр отчета Allure
В том же (втором) окне терминала введите одну из команд для открытия отчета.
- Для macOS или Linux:
```bash
./mvnw allure:serve
```
- Для Windows (PowerShell/CMD):
```bash
.\mvnw.cmd allure:serve
```

